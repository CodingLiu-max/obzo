# 使用方法

显式调用：`使用 OBZO`，然后说明目标目录和任务。用户给出的范围优先；未给出时，Skill 使用 `3–7` 个路线节点、`8–15` 篇核心论文、最多 `3` 类检索源的默认边界。

## 推荐参数

尽量提供以下信息；不需要全部填写：

```text
模式：扩充学习 / 扩充工程 / 新建体系 / 加入指定论文 / 审计
主题：
目标与受众：
Obsidian 根目录：
必须包含：
明确排除：
时间范围：
论文数量上限：
会议、期刊或数据库限制：
路线深度：概览 / 理论 / 推导 / 实现 / 实验
是否允许新建或调整目录：
Zotero：仅分类 / 元数据 / 下载 PDF / 全部
保留要求：哪些原文、命名和结构不能改变
```

## 扩充现有学习路线

```text
使用 OBZO 扩充我的学习体系。
Obsidian 目录是：[实际路径]。
目标是：[学习完成后能解释/推导/实现什么]。
请先检查现有学习里途、相关论文和推导笔记，再补齐权威路线。
范围包含：[分支]；排除：[分支]；核心论文最多 [数量] 篇；时间截至 [日期]。
保留我的原文，只在合适位置增加可读链接和教学说明。
每篇论文在核心方法前增加“领域背景与技术缺陷”，只依据现有笔记和论文材料，不新增探索；材料不足处标记“需回看原文引言/实验”。
在 Zotero 建立对应层级，去重、补全元数据并下载可合法获取的原始 PDF。
```

## 新建完整知识体系

```text
使用 OBZO 为 [主题] 构建完整但精简的知识体系。
面向：[受众]；目标：[理论/实现/工程/研究选题]。
Obsidian 根目录：[实际路径]；允许创建目录和索引。
路线范围：[必须包含]；不包含：[明确排除]。
将路线拆成 3–7 个依赖节点，每个节点只选择必要的奠基、代表和当前论文，总数不超过 [数量]。
为每个节点增加学习目标、前置知识、论文作用、推导或实践任务、常见误区和通过标准。
同步建立对应 Zotero 集合、论文条目、PDF 附件和双向链接。
```

## 完善工程路线

```text
使用 OBZO 完善这个工程文件：[实际路径]。
工程目标：[交付物]；约束：[数据、算力、框架、时间]。
重点检索：[任务/方法/数据集/指标]；基线必须包含：[名称]；论文上限：[数量]。
请补齐权威依据、最近工作、数据与统一评测、模块接口、实现里程碑、基线、消融、风险和失败判据。
论文存到工程的相关论文目录，并同步到对应 Zotero 层级；不要创建 Git，除非我明确要求。
```

## 加入指定论文

```text
使用 OBZO 加入以下论文：[标题、DOI、arXiv 或链接]。
目标目录：[实际路径]；应放在路线节点：[节点]。
请先去重和核验正式发表版本，再建立 Model-Year-Venue 笔记、路线链接、Zotero 条目和原始 PDF 附件。
不要扩展到未指定的论文。
```

## 审计现有体系

```text
使用 OBZO 审计：[实际路径]。
只检查，不自动大规模扩充。
检查路线是否缺少奠基、核心分支、统一理论、基准评测或当前代表论文；检查论文权威性、重复、断链、元数据、Zotero 分类和 PDF 附件。
按“必须修复 / 建议补充 / 可选阅读”给出结果，并说明检索边界。
```

## 默认交付说明

完成后应报告：

- 修改或创建了哪些 Obsidian 文件；
- 路线节点和核心论文数量；
- 使用的检索范围、来源类别和停止条件；
- Zotero 集合、条目和 PDF 状态；
- 未解决的元数据、证据或路线缺口；
- 下一项最值得执行的学习、推导或工程检查点。
